#include "main.h"

/**
 * _isdigit - prints digit from zero to nine
 *
 * @c: parameter to be checked
 *
 * Return: returns (1) and (0)
 */
int _isdigit(int c)

{
if (c >= 0 && c <= 9)
return (1);
else
return (0);

}
