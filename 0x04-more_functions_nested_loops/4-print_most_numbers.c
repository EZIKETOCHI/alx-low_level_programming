#include "main.h"

/**
 * _most_numbers - prints digit except (2) and (4)
 *
 * @i: parameter to be checked 
 *
 * Return: Always (0)
 */
void print_most_numbers(void)
{
int i
for (i = 0 ; i <= 9 ; i++)
{
if (i != 2) && (i != 4)
{
putchar;
}
putchar ('\n');
return (0);
}
